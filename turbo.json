{
  "$schema": "https://turbo.build/schema.json",
  "tasks": {
    "build": {
      "dependsOn": ["^build"],
      "outputs": ["dist/**"],
      "inputs": ["src/**"],
      "cache": true
    },
    "build:dts": {
      "dependsOn": ["^build:dts"],
      "outputs": ["dist/**","temp/**"]
    },
    "build:all": {
      "dependsOn": ["^build:all"],
      "outputs": ["dist/**","temp/**"]
    },
    "check-types": {
      "dependsOn": ["^check-types"]
    },
    "dev": {
      "persistent": true,
      "cache": false
    },
    "check": {
      "dependsOn": ["^build"],
      "inputs": [
        "src/**",
        "tsconfig.json"
      ]
    },
    "format:check": {
      "inputs": [
        "src/**/*.{ts,tsx,.vue}"
      ]
    }
  }
}
